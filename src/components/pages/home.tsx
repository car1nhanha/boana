import { DefaultTemplate } from "../templates/default";

export const HomePage = () => {
  return (
    <DefaultTemplate>
      <h1>Home Page</h1>
      <p>Welcome to the Home Page!</p>
    </DefaultTemplate>
  );
};
